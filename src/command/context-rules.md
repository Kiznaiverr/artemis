# Context Rules

Context limits:

- Only use comment windows around each candidate peak.
- Only use subtitle snippets inside the same peak window.
- Never send the full subtitle file.
- Keep subtitle text trimmed to the smallest useful window.
- If the subtitle snippet is too long, shorten it around the peak timestamp.
- If subtitles are unavailable, continue with comments only.
